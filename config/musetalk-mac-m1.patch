--- a/server.py
+++ b/server.py
@@ -108,7 +108,12 @@ async def lifespan(_app: FastAPI):
     whisper = whisper.to(device=device, dtype=weight_dtype).eval()
     whisper.requires_grad_(False)
 
-    fp = FaceParsing(left_cheek_width=90, right_cheek_width=90)
+    left_cheek_width = int(os.environ.get("MUSETALK_LEFT_CHEEK_WIDTH", "60"))
+    right_cheek_width = int(os.environ.get("MUSETALK_RIGHT_CHEEK_WIDTH", "60"))
+    fp = FaceParsing(
+        left_cheek_width=left_cheek_width,
+        right_cheek_width=right_cheek_width,
+    )
     timesteps = torch.tensor([0], device=device)
 
     state.update(
@@ -121,8 +126,17 @@ async def lifespan(_app: FastAPI):
         weight_dtype=weight_dtype,
         fp=fp,
         timesteps=timesteps,
+        mask_mode=os.environ.get("MUSETALK_PARSING_MODE", "jaw"),
+        upper_boundary_ratio=float(
+            os.environ.get("MUSETALK_UPPER_BOUNDARY_RATIO", "0.55")
+        ),
+    )
+    print(
+        "[musetalk] ready "
+        f"mask={state['mask_mode']} cheeks={left_cheek_width}/{right_cheek_width} "
+        f"upper={state['upper_boundary_ratio']}",
+        flush=True,
     )
-    print("[musetalk] ready", flush=True)
     yield
 
 
@@ -195,7 +209,7 @@
 
 # ─── helpers ──────────────────────────────────────────────────
 
-EXTRA_MARGIN = 10  # v15 default
+EXTRA_MARGIN = int(os.environ.get("MUSETALK_EXTRA_MARGIN", "6"))
 
 
 def _detect_media_kind(data: bytes) -> str:
@@ -260,7 +274,11 @@ def prepare_avatar(video_bytes: bytes) -> dict:
             # The mask depends only on the source frame, not on the generated
             # mouth — so we do this once during warmup, saving ~7s per lipsync.
             mask_array, crop_box = get_image_prepare_material(
-                frame, [x1, y1, x2, y2], fp=fp, mode="jaw"
+                frame,
+                [x1, y1, x2, y2],
+                fp=fp,
+                mode=state["mask_mode"],
+                upper_boundary_ratio=state["upper_boundary_ratio"],
             )
             mask_list.append(mask_array)
             crop_box_list.append(crop_box)
@@ -270,6 +288,7 @@ def prepare_avatar(video_bytes: bytes) -> dict:

         return {
             "fps": fps,
+            "is_static_source": len(img_list) == 1,
             "coord_list_cycle": valid_coords + valid_coords[::-1],
             "frame_list_cycle": valid_frames + valid_frames[::-1],
             "input_latent_list_cycle": input_latent_list + input_latent_list[::-1],
@@ -280,8 +298,10 @@ def prepare_avatar(video_bytes: bytes) -> dict:
         shutil.rmtree(tmp, ignore_errors=True)
 
 
-def run_lipsync(avatar: dict, audio_bytes: bytes, batch_size: int = 16) -> bytes:
+def run_lipsync(avatar: dict, audio_bytes: bytes, batch_size: int | None = None) -> bytes:
     """Given a prepared avatar and audio bytes, return final mp4 bytes."""
+    if batch_size is None:
+        batch_size = int(os.environ.get("MUSETALK_BATCH_SIZE", "4"))
     prof = {}
     _t0 = time.time()
 
@@ -315,6 +336,11 @@ def run_lipsync(avatar: dict, audio_bytes: bytes, batch_size: int = 16) -> bytes:
         latent_cycle = avatar["input_latent_list_cycle"]
         mask_cycle = avatar["mask_list_cycle"]
         crop_box_cycle = avatar["crop_box_list_cycle"]
+        temporal_smoothing = (
+            float(os.environ.get("MUSETALK_TEMPORAL_SMOOTHING", "0.12"))
+            if avatar.get("is_static_source")
+            else 0.0
+        )

         gen = datagen(whisper_chunks, latent_cycle, batch_size, 0, device)
         res_frame_list = []
@@ -354,6 +380,7 @@ def run_lipsync(avatar: dict, audio_bytes: bytes, batch_size: int = 16) -> bytes:
         assert ff_proc.stdin is not None
         blend_ms = 0
         pipe_ms = 0
+        previous_face = None
         for i, res_frame in enumerate(res_frame_list):
             idx = i % len(coord_cycle)
             x1, y1, x2, y2 = coord_cycle[idx]
@@ -362,6 +389,15 @@ def run_lipsync(avatar: dict, audio_bytes: bytes, batch_size: int = 16) -> bytes:
                 rs = cv2.resize(res_frame.astype(np.uint8), (x2 - x1, y2 - y1))
             except Exception:
                 continue
+            if previous_face is not None and temporal_smoothing > 0:
+                rs = cv2.addWeighted(
+                    rs,
+                    1.0 - temporal_smoothing,
+                    previous_face,
+                    temporal_smoothing,
+                    0,
+                )
+            previous_face = rs
             _t2 = time.time()
             combined = get_image_blending(
                 ori, rs, [x1, y1, x2, y2],
@@ -419,7 +439,18 @@ class StreamReq(BaseModel):
 
 @app.get("/health")
 def health():
-    return {"ok": True, "device": str(state.get("device")), "avatars": list(avatar_cache)}
+    return {
+        "ok": True,
+        "device": str(state.get("device")),
+        "avatars": list(avatar_cache),
+        "blend": {
+            "extra_margin": EXTRA_MARGIN,
+            "mode": state.get("mask_mode"),
+            "left_cheek_width": int(os.environ.get("MUSETALK_LEFT_CHEEK_WIDTH", "60")),
+            "right_cheek_width": int(os.environ.get("MUSETALK_RIGHT_CHEEK_WIDTH", "60")),
+            "upper_boundary_ratio": state.get("upper_boundary_ratio"),
+        },
+    }
 
 
 @app.post("/warmup")
