# ComfyUI runtime data

ComfyUI Desktop installs its local Python environment, models, custom nodes and
runtime files in this project-owned directory. Large generated/model files are
ignored by the repository rules and should not be committed.
